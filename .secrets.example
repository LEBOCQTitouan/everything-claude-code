# Secrets for local CI testing with act
# Copy to .secrets and fill in values: cp .secrets.example .secrets

# npm registry token (required for release.yml publish-npm job)
NPM_TOKEN=your_npm_token_here
