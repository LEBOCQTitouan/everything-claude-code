//! Tests for cartography hook handlers — delta_helpers module scope.
//!
//! Previously included `start_test_helpers`, `delta_test_helpers`, and
//! `stop_test_helpers` which tested the legacy `process_cartography` pipeline.
//! Those test modules have been removed along with the dead code (PC-031).
