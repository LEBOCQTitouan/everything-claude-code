[package]
name = "ecc-test-support"
description = "Test doubles — in-memory filesystem, mock executor, mock environment"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
ecc-ports = { workspace = true }
ecc-domain = { workspace = true }
