# Architecture: [Project Name]

<!-- Generated by doc-generator | Date: YYYY-MM-DD -->
<!-- Template: C4 Model — select applicable levels based on codebase size -->

## Overview

[1-2 sentence description of what the system does and who it serves.]

**Tech stack:** [language, framework, database, key dependencies]
**Codebase size:** [small/medium/large] ([N] source files, [M] modules)

---

## System Context (L1)

> Skip for small/medium codebases unless external integrations exist.

[System Name] interacts with the following external actors and systems:

| Actor/System | Type | Interaction |
|-------------|------|-------------|
| [User] | Actor | [what they do with the system] |
| [External API] | System | [what data flows, which direction] |
| [Database] | Store | [what data is stored] |

```mermaid
graph TB
    %% Actors
    %% Systems
    %% Stores
    %% Edges
```

---

## Container View (L2)

> Skip for small codebases.

| Container | Technology | Purpose |
|-----------|-----------|---------|
| [API Server] | [Node.js + Express] | [Serves REST API] |
| [Worker] | [Node.js] | [Background job processing] |
| [Database] | [PostgreSQL] | [Primary data store] |

```mermaid
graph TB
    %% Containers and their connections
```

---

## Component View (L3)

> The core view — always generate this.

### [Module 1: name] — [one-line purpose]

**Key exports:** [top 3-5 exports]
**Depends on:** [module names]
**Depended on by:** [module names]

### [Module 2: name] — [one-line purpose]

**Key exports:** [top 3-5 exports]
**Depends on:** [module names]
**Depended on by:** [module names]

```mermaid
graph LR
    %% Module dependency graph
```

---

## Code View (L4)

> Only for complex modules. Skip if component view is sufficient.

### [Complex Module Name]

[Class hierarchy, state machine, or data flow for this module]

```mermaid
%% classDiagram, stateDiagram, or sequence diagram as appropriate
```

---

## Cross-Cutting Concerns

### Error Handling
[How errors propagate: throw → catch boundaries, error types, user-facing vs internal]

### Configuration
[How config flows: env vars → config module → consumers]

### Testing Strategy
[Test types per layer: unit for lib, integration for API, e2e for critical flows]

---

## Decision Log

> Link to ADRs if they exist, otherwise note key architectural decisions inline.

| Date | Decision | Rationale |
|------|----------|-----------|
| [date] | [what was decided] | [why] |
