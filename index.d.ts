/**
 * @lebocqtitouan/ecc — Claude Code configuration manager
 *
 * This is a CLI-only package. No programmatic Node.js API is exposed.
 *
 * Install:  npm install -g @lebocqtitouan/ecc
 * Usage:    ecc <command> [options]
 *
 * Commands:
 *   ecc install <language...>              Global Claude setup → ~/.claude/
 *   ecc init [--template <name>] [lang]    Per-project CLAUDE.md + hooks
 *   ecc completion                         Set up shell tab-completion
 *   ecc help [command]                     Show usage
 *
 * Supported languages:  golang | python | rust | swift | typescript
 * Supported templates:  django-api | go-microservice | rust-api | saas-nextjs
 */
export {};
